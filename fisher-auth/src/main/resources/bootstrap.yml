spring:
  application:
    name: fisher-auth-service
  cloud:
    nacos:
      config:
        server-addr: 47.92.174.87:8848
        file-extension: yml
        group: AUTH_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

